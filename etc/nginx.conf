server {
	server_name _;

	root /home/box/web;


	location / {
	    try_files $uri $uri/ =404;
	}

	location /uploads/ {
	    root /home/box/web;
	}

	location ~ \.(\w)$  {
	    root /home/box/web/public;
	}
}